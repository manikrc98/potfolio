{
  "name": "potfolio-server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd template && npm install && npm run build",
    "bundle": "node scripts/bundle-templates.js",
    "dev": "npm run bundle && wrangler dev",
    "deploy": "npm run bundle && wrangler deploy"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.98.0",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
